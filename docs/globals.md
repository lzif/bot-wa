# Global Variables

This project uses a global object to provide easy access to certain modules. The following global variables are available:

## `config`

An object containing the application's configuration. This is an export from the `src/config` directory.

## `tools`

An object containing various utility functions. This is an export from the `src/tools` directory. See `docs/tools.md` for a detailed explanation of each function.
